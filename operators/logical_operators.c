#include <stdio.h>

// Learning logical operators

int main()
{
	// ! && || 
	int x = 0;
	int y = -2;
	int z = 5;

	printf("!x: %d\n", !x);
	printf("x && y: %d\n", x && y);
	printf("x || y: %d\n", x || y);

	return (0);
}
