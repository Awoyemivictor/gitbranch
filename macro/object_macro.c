#include <stdio.h>
#define PI 3.1415

// Printing the Area of a cirle

int main()
{
	float r, area;

	printf("Enter the value of radius: ");
	scanf("%f", &r);

	//see here is the use of macro PI
	printf("Area = %.2f", area);

	return (0);
}
