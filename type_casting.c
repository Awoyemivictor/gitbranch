#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0
 */

int main()
{
	int x = 3;
	int y = 2;

	// (target type) <variable or valid expression>
	float result = (float)x / y;

	printf("Result: %f\n", result);
}
